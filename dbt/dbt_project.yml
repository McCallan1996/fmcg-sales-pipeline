name: fmcg_analytics
version: "1.0.0"
config-version: 2

profile: fmcg_analytics

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
packages-install-path: "/tmp/dbt_packages"
clean-targets:
  - "target"
  - "/tmp/dbt_packages"

models:
  fmcg_analytics:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
